services:
  - type: web
    name: stocks-rsps-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --config gunicorn_config.py stock_dashboard_backend:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0

